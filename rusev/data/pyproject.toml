[project]
name = "data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=3.1.0",
    "fire>=0.7.0",
    "jsonlines>=4.0.0",
]

[tool.pyright]
typeCheckingMode = "basic"
